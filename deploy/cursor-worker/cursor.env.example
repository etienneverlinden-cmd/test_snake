# Cursor headless worker (Hetzner)
# Fill CURSOR_API_KEY then: systemctl enable --now cursor-worker

CURSOR_API_KEY=
CURSOR_INBOX=/opt/cursor-worker/inbox
CURSOR_OUTBOX=/opt/cursor-worker/outbox
CURSOR_WORK_DIR=/opt/cursor-worker/work
CURSOR_LOG_DIR=/opt/cursor-worker/logs
CURSOR_POLL_SECONDS=15
