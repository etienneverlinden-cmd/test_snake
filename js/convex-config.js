// Public Convex deployment URL (safe to expose in the browser).
// After `npx convex dev` or `npx convex deploy`, paste the URL here, e.g.:
//   window.STIJN_ARCADE_CONVEX_URL = "https://happy-animal-123.convex.cloud";
// Leave empty to use localStorage only (pawnie-style fallback).
window.STIJN_ARCADE_CONVEX_URL = "";
