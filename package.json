{
  "name": "stijn-arcade",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev:convex": "convex dev",
    "deploy:convex": "convex dev --once",
    "dev:site": "python -m http.server 8080",
    "auth:keys": "node scripts/set-auth-keys.mjs",
    "build:pages": "node scripts/build-pages.mjs",
    "deploy:pages": "npm run build:pages && wrangler pages deploy dist --project-name=pragmatict"
  },
  "dependencies": {
    "@auth/core": "0.41.1",
    "@convex-dev/auth": "^0.0.90",
    "@oslojs/crypto": "^1.0.1",
    "convex": "^1.42.1",
    "jose": "^6.0.11"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}
